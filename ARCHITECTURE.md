# 🏗️ AskDB System Architecture

## High-Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │             STREAMLIT FRONTEND (app.py)                    │  │
│  │                                                            │  │
│  │  • File Upload (CSV/Excel/SQLite)                         │  │
│  │  • Natural Language Input                                 │  │
│  │  • Results Visualization                                  │  │
│  │  • Chat History                                           │  │
│  │  • Schema Graph Display                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ REST API (optional)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FASTAPI BACKEND                               │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    API ROUTES                             │  │
│  │                                                            │  │
│  │  /api/auth/signup, /login    ← JWT Authentication        │  │
│  │  /api/upload                 ← File Upload Handler       │  │
│  │  /api/schema/{session_id}    ← Schema Extraction         │  │
│  │  /api/query                  ← NL2SQL Execution          │  │
│  │  /api/chats                  ← Chat Management           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                   │
│  ┌──────────────┬─────────────┬────────────┬──────────────┐   │
│  │   auth.py    │  upload.py  │  nl2sql.py │ query_runner │   │
│  │              │             │            │              │   │
│  │  • JWT Token │  • Session  │  • Schema  │  • Safe SQL  │   │
│  │  • bcrypt    │  • Temp DB  │  • Template│  • Validate  │   │
│  │  • OAuth2    │  • Cleanup  │  • AI Model│  • Execute   │   │
│  └──────────────┴─────────────┴────────────┴──────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                    │                         │
                    │                         │
          ┌─────────▼─────────┐    ┌─────────▼──────────┐
          │   POSTGRESQL      │    │   TEMP SQLITE      │
          │   (Permanent)     │    │   (Per Session)    │
          │                   │    │                    │
          │  ┌─────────────┐ │    │  ┌──────────────┐ │
          │  │   users     │ │    │  │user_data.db  │ │
          │  │   chats     │ │    │  │  (uploaded)  │ │
          │  │   messages  │ │    │  └──────────────┘ │
          │  │   logs      │ │    │                    │
          │  └─────────────┘ │    │  Auto-cleanup      │
          │                   │    │  after 24 hours    │
          └───────────────────┘    └────────────────────┘
           Neon PostgreSQL           /temp/askdb_uploads
```

---

## Component Interaction Flow

### 1. User Uploads Database

```
User
  │
  ├── Selects file (CSV/Excel/SQLite)
  │
  ▼
Streamlit Frontend
  │
  ├── Validates file type
  ├── Reads file bytes
  │
  ▼
Backend Upload Handler (upload.py)
  │
  ├── Creates unique session ID
  ├── Saves to temp directory
  ├── Extracts schema (PRAGMA)
  │
  ▼
Returns
  ├── session_id: "ae3a8f69-..."
  ├── tables: ["employees", "departments"]
  └── path: "/temp/askdb_uploads/ae3a8f69....sqlite"
```

### 2. User Asks Question

```
User enters: "Show employees with salary > 75000"
  │
  ▼
NL2SQL Engine (nl2sql.py + core.py)
  │
  ├── Parse question
  ├── Load schema from uploaded DB
  ├── Try template match (fast path)
  ├── If no match → use AI model (T5)
  │
  ▼
Generated SQL:
  SELECT * FROM "employees" WHERE "salary" > 75000
  │
  ▼
Query Runner (query_runner.py)
  │
  ├── Validate query (SELECT only)
  ├── Execute on uploaded SQLite DB
  ├── Return pandas DataFrame
  │
  ▼
Results displayed in Streamlit
  ├── Table view
  ├── Auto charts (if numeric data)
  └── Summary statistics
```

### 3. Conversation Logging

```
Every Query
  │
  ├── User message saved to PostgreSQL
  ├── SQL query saved (server-side only)
  ├── Result metadata saved
  │   ├── rows_returned: 3
  │   ├── success: true
  │   └── timestamp
  │
  ▼
PostgreSQL (messages table)
  ├── Persistent storage
  ├── Full history
  └── Can resume chat sessions
```

---

## Data Flow Diagram

```
┌────────────┐
│   USER     │
└─────┬──────┘
      │ 1. Upload CSV
      ▼
┌─────────────────┐
│   STREAMLIT     │
│    Frontend     │
└─────┬───────────┘
      │ 2. File bytes
      ▼
┌─────────────────┐       ┌──────────────────┐
│   BACKEND       │       │   TEMP SQLITE    │
│   Upload API    │──────▶│   user_data.db   │
└─────┬───────────┘   3.  └──────────────────┘
      │ Create
      │ session_id
      ▼
┌─────────────────┐
│   PostgreSQL    │
│   Log upload    │
└─────────────────┘

      USER
        │ 4. Ask: "Show all"
        ▼
┌─────────────────┐
│   NL2SQL        │
│   Engine        │
└─────┬───────────┘
      │ 5. Generate SQL
      ▼
┌─────────────────┐       ┌──────────────────┐
│  Query Runner   │──────▶│   TEMP SQLITE    │
│  execute_query  │   6.  │   Read data      │
└─────┬───────────┘       └──────────────────┘
      │ 7. pandas DF
      ▼
┌─────────────────┐
│   PostgreSQL    │
│   Save message  │
│   + SQL + results│
└─────────────────┘
      │ 8. Return to user
      ▼
┌─────────────────┐
│   STREAMLIT     │
│   Display       │
│   • Table       │
│   • Charts      │
│   • Summary     │
└─────────────────┘
```

---

## Security Layers

```
┌──────────────────────────────────────────────────────┐
│                  SECURITY LAYERS                      │
└──────────────────────────────────────────────────────┘

Layer 1: Authentication
┌────────────────────────────────────────┐
│  • JWT tokens (7-day expiration)       │
│  • bcrypt password hashing             │
│  • Protected routes                    │
└────────────────────────────────────────┘

Layer 2: Input Validation
┌────────────────────────────────────────┐
│  • Pydantic schemas                    │
│  • File type validation                │
│  • SQL injection prevention            │
└────────────────────────────────────────┘

Layer 3: Query Safety
┌────────────────────────────────────────┐
│  • Read-only execution                 │
│  • No INSERT/UPDATE/DELETE/DROP        │
│  • Error message sanitization          │
└────────────────────────────────────────┘

Layer 4: Data Isolation
┌────────────────────────────────────────┐
│  • Separate databases (PG + SQLite)    │
│  • Unique session IDs                  │
│  • No data mixing between users        │
│  • Auto-cleanup (24 hours)             │
└────────────────────────────────────────┘
```

---

## Database Schema

### PostgreSQL (Permanent Storage)

```sql
┌────────────────────────────────────────┐
│              users                      │
├────────────────────────────────────────┤
│ id              INTEGER PK              │
│ username        VARCHAR(100) UNIQUE     │
│ email           VARCHAR(255) UNIQUE     │
│ password_hash   VARCHAR(255)            │
│ display_name    VARCHAR(200)            │
│ created_at      TIMESTAMP               │
└────────────────────────────────────────┘
              │
              │ 1:M
              ▼
┌────────────────────────────────────────┐
│              chats                      │
├────────────────────────────────────────┤
│ id              INTEGER PK              │
│ user_id         INTEGER FK → users.id   │
│ title           VARCHAR(500)            │
│ created_at      TIMESTAMP               │
│ updated_at      TIMESTAMP               │
└────────────────────────────────────────┘
              │
              │ 1:M
              ▼
┌────────────────────────────────────────┐
│            messages                     │
├────────────────────────────────────────┤
│ id              INTEGER PK              │
│ chat_id         INTEGER FK → chats.id   │
│ role            VARCHAR(20)             │
│ content         TEXT                    │
│ sql_query       TEXT                    │
│ rows_returned   INTEGER                 │
│ success         INTEGER                 │
│ created_at      TIMESTAMP               │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│              logs                       │
├────────────────────────────────────────┤
│ id              INTEGER PK              │
│ user_id         INTEGER FK (nullable)   │
│ chat_id         INTEGER FK (nullable)   │
│ level           VARCHAR(20)             │
│ event_type      VARCHAR(100)            │
│ message         TEXT                    │
│ meta_data       TEXT                    │
│ created_at      TIMESTAMP (indexed)     │
└────────────────────────────────────────┘
```

### SQLite (Temporary, User-Uploaded)

```
Dynamic schema based on user data
Example:
┌────────────────────────────────────────┐
│           employees                     │
├────────────────────────────────────────┤
│ id              INTEGER                 │
│ name            TEXT                    │
│ department      TEXT                    │
│ salary          REAL                    │
│ age             INTEGER                 │
│ city            TEXT                    │
└────────────────────────────────────────┘

Storage: /temp/askdb_uploads/{session_id}.sqlite
Lifecycle: 24 hours → auto-delete
```

---

## Technology Stack

```
┌──────────────────────────────────────────────────────┐
│                  BACKEND                              │
├──────────────────────────────────────────────────────┤
│  Framework:       FastAPI 0.104+                     │
│  Web Server:      Uvicorn 0.24+                      │
│  Authentication:  python-jose (JWT)                  │
│  Password:        passlib (bcrypt)                   │
│  ORM:             SQLAlchemy 2.0                     │
│  Database:        PostgreSQL (psycopg2)              │
│  Validation:      Pydantic 2.0+                      │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                  FRONTEND                             │
├──────────────────────────────────────────────────────┤
│  Framework:       Streamlit 1.51+                    │
│  Visualization:   Plotly 6.4+                        │
│  Data:            Pandas 2.3+                        │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                   AI/ML                               │
├──────────────────────────────────────────────────────┤
│  Model:           T5-base (Hugging Face)             │
│  Framework:       PyTorch 2.9+                       │
│  Tokenizer:       Transformers 4.30+                 │
│  Fallback:        Template engine (core.py)          │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                  DATABASE                             │
├──────────────────────────────────────────────────────┤
│  Production:      Neon PostgreSQL (cloud)            │
│  User Data:       SQLite 3 (temp files)              │
│  Fallback:        SQLite (local dev)                 │
└──────────────────────────────────────────────────────┘
```

---

## Deployment Options

### Option 1: Streamlit Standalone
```
┌─────────────────┐
│   Streamlit     │ ← Users connect here
│   (Port 8501)   │
└────────┬────────┘
         │
         ├── Direct PostgreSQL connection
         └── Local file processing
```

### Option 2: Full Stack
```
┌─────────────────┐
│   Streamlit     │ ← UI
│   (Port 8501)   │
└────────┬────────┘
         │ REST API
         ▼
┌─────────────────┐
│    FastAPI      │ ← Backend
│   (Port 8000)   │
└────────┬────────┘
         │
         ├── PostgreSQL (port 5432)
         └── Temp SQLite files
```

### Option 3: Microservices (Future)
```
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Frontend │───▶│   API    │───▶│PostgreSQL│
└──────────┘    │ Gateway  │    └──────────┘
                └─────┬────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
        ▼             ▼             ▼
  ┌─────────┐   ┌─────────┐   ┌─────────┐
  │  Auth   │   │ NL2SQL  │   │  Query  │
  │ Service │   │ Service │   │ Service │
  └─────────┘   └─────────┘   └─────────┘
```

---

## Performance Characteristics

```
┌─────────────────────────────────────────────────┐
│              RESPONSE TIMES                      │
├─────────────────────────────────────────────────┤
│  Database Upload:      < 1s  (CSV < 10MB)       │
│  Schema Extraction:    < 100ms                  │
│  Template SQL:         < 50ms                   │
│  AI Model SQL:         1-3s (first query)       │
│  Query Execution:      < 500ms (10k rows)       │
│  Total (Template):     < 1s                     │
│  Total (AI Model):     2-4s                     │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│              SCALABILITY                         │
├─────────────────────────────────────────────────┤
│  Concurrent Users:     100+ (with FastAPI)      │
│  Max Upload Size:      100MB (configurable)     │
│  Max Query Results:    100k rows               │
│  Session Storage:      Limited by disk space    │
└─────────────────────────────────────────────────┘
```

---

**Built with ❤️ for secure, scalable NL2SQL queries**

